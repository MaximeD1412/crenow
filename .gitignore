# ── Node / pnpm / Turbo ──
node_modules/
.turbo/
dist/
*.tsbuildinfo

# Generated API client (produced from shared/openapi/openapi.yaml by `pnpm generate`)
shared/openapi/src/generated/

# ── Gradle / Java ──
.gradle/
backend/build/
backend/.gradle/
# Output of the IDE's Java language server (Eclipse/VS Code), not Gradle's
backend/bin/

# ── Expo / React Native ──
.expo/
apps/mobile/.expo/

# ── Env / secrets ──
.env
.env.local
*.local

# ── Editor / OS ──
.idea/
.vscode/
.DS_Store
